# ADR-004: 前后端分离架构

**状态**: 已接受  
**上下文**: 平台需要同时支持数据密集的报表页面和交互丰富的图表操作。  
**决策**: 采用前后端分离架构，后端 FastAPI 提供 REST API，前端 React SPA。  
**理由**: 
- 图表计算和后端逻辑与展示解耦，可独立迭代
- FastAPI 天然适合数据密集型场景的异步处理
- 前后端分离利于未来可能的 API 开放
**后果**: 需要处理 CORS；需要明确定义 API 契约。
